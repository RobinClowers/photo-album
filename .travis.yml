dist: xenial
matrix:
  include:
    - language: ruby
      cache: bundler
      before_script: psql -c 'create database travis_ci_test;' -U postgres
      script: bundle exec rake
      services:
        - postgresql
        - redis-server
    - language: node_js
      node_js: 11
      cache: yarn
      script:
        - cd client/
        - yarn install
        - yarn test


